# ============================================================================
# API KEYS — never commit
# ============================================================================
# generate.py contains the live API keys (lines 75-76). The skill needs those
# keys IN the file to work via claude.ai / Claude desktop (no environment
# variables are available there). That's why the working script is NEVER
# tracked. The repo only contains generate.example.py without keys.
generate.py

# macOS
.DS_Store

# remember-skill session buffer (local state, not part of the skill)
.remember/

# Multi-turn session state (runtime, per project folder)
.image-sessions/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Generated output (timestamped images + .txt sidecars with prompts)
*.png
*.jpg
*.jpeg
*.webp
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]_*.txt

# Setup-script output: bundle contains API keys, NEVER commit
*.skill
image.zip
